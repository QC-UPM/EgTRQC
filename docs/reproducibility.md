# Reproducibility Notes

The maintained documentation now lives in the Sphinx site under `docs/source/`.

Build it with:

`make -C docs html`

The rendered HTML entry point is generated at `docs/_build/html/index.html`.
